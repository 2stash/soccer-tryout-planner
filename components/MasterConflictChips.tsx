type Props = {
  playerId: string;
  /** Prefix each label with "Also " (default true). */
  alsoPrefix?: boolean;
  compact?: boolean;
};

/** Stubbed: shared workspace has no cross-master conflict chips. */
export function MasterConflictChips(_props: Props) {
  return null;
}
